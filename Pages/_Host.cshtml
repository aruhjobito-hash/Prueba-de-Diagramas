@page "/"
@namespace LeonXIIICore.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}

@* <script src="_framework/blazor.server.js"></script>
 <script>
    // Your existing download function
    window.descargarArchivo = function (url) {
        var a = document.createElement('a');
        a.href = url;
        a.download = '';
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
    }

    // Modified Blazor initialization
    function initializeBlazor() {
        // Only initialize Blazor if we're not in the Angular iframe context
        if (window === window.parent || !window.location.pathname.startsWith('/angular')) {
            Blazor.start({
                configureNavigation: function (options) {
                    // Configure navigation to ignore Angular routes
                    options.listenForNavigationEvents = !window.location.pathname.startsWith('/angular');
                }
            });
        }
    }

    // Initialize when DOM is loaded
    document.addEventListener('DOMContentLoaded', initializeBlazor);
</script>
 *@

<script>
    window.descargarArchivo = function (url) {
        var a = document.createElement('a');
        a.href = url;
        a.download = '';

        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
    }

</script>



<component type="typeof(App)" render-mode="Server" />
@* <component type="typeof(App)" render-mode="ServerPrerendered" /> *@