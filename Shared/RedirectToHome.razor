@inject NavigationManager Navigation

@code {
    protected override async void OnInitialized()
    {
        await Task.Delay(5000);// Wait 5 sec to redirect to Home
        Navigation.NavigateTo("/home", true);
    }
}
